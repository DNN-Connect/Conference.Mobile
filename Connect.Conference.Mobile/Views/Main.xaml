<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:local="clr-namespace:Connect.Conference.Mobile.Views;assembly=Connect.Conference.Mobile"
                  x:Class="Connect.Conference.Mobile.Views.Main">
   <StackLayout VerticalOptions="StartAndExpand">
    <ListView x:Name="listView" VerticalOptions="StartAndExpand">
     <ListView.ItemTemplate>
      <DataTemplate>
       <TextCell Text="{Binding Name}" Detail="{Binding Location}" />
      </DataTemplate>
     </ListView.ItemTemplate>
    </ListView>
    <StackLayout
     VerticalOptions="StartAndExpand"
     HorizontalOptions="CenterAndExpand"
     Padding="20,80,20,20">
     <Button x:Name="btnAdd" Text="Add Conference" BackgroundColor="#4A83AD" TextColor="White"></Button>
    </StackLayout>
   </StackLayout>
</ContentPage>
